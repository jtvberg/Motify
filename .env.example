# Spotify API Configuration
# Copy this file to .env and replace with your actual Spotify app credentials
# Get these from: https://developer.spotify.com/dashboard

# Your Spotify app's Client ID (safe to expose to browser)
PUBLIC_SPOTIFY_CLIENT_ID=your_spotify_client_id_here

# Your app's redirect URI (must match what's configured in Spotify app)
# MUST use loopback address 127.0.0.1 as required by Spotify's security policy
PUBLIC_SPOTIFY_REDIRECT_URI=http://127.0.0.1:8181/callback

# Note: Do NOT include SPOTIFY_CLIENT_SECRET in client-side applications
# The client secret should never be exposed to the browser for security reasons
